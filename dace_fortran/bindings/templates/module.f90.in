! AUTO-GENERATED by dace.frontend.hlfir.bindings -- do not edit.
! Source: {entry} @ frozen signature version {schema_version}
module {entry}_dace_bindings
  ! ``, intrinsic`` forces the real intrinsic module even when a USE-imported
  ! source defines a same-named non-intrinsic ``iso_c_binding`` (e.g. a
  ! flattened/pruned single-file checkpoint that stubs it): the wrapper needs
  ! the real ``c_loc`` / ``c_ptr`` / ``c_int`` regardless.
  use, intrinsic :: iso_c_binding
{use_statements}
  implicit none
  private
  public :: {entry}_dace, {entry}_dace_finalize

{c_interface}

{handle_state}

contains

{wrapper_body}

{finalize_body}

end module {entry}_dace_bindings
